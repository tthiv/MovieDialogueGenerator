Thivija Thavarajah

Character Dialogue Generator

text.data -> directory with all text files used
model.data -> directory the outputs the model produces

CharacterDialogue.ipynb -> Generates the character dialogue based on the outputed textfiles from readScript.ipynb
ReadScript.ipynb -> Reads and preprocess the scripts and gets the sentences containing emotional text and stores them as text files.